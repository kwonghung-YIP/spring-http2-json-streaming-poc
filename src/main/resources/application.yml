server:
  http2:
    enabled: true

logging:
  level:
    reactor.netty: debug
    io.netty: debug
    
---
spring:  profiles:  - local
  
server:
  port: 8443  ssl: 
    key-store: classpath:local.p12
    key-store-type: PKCS12
    key-store-password: abcd1234
    key-alias: baeldung

---
spring:  profiles:  - k8s

server:  port: 8080